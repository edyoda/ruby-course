<table class="table table-striped">
  <tr>
    <th>Name</th>
    <th>Email</th>
    </tr>

      <tr>
        <td><%= @user.name %></td>
        <td><%= @user.email %></td>
        <td><%= link_to "Show", user_path(@user.id) %></td>
      </tr>

</table>


<%= link_to "Add User", new_user_path%>
